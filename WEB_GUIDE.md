# 終極智能教室座位表生成器 - 網頁版使用指南

## 🌟 特色功能

### ✅ 無需安裝
- 純網頁版本，開啟瀏覽器即可使用
- 不需要安裝Python或任何軟體
- 支援所有現代瀏覽器（Chrome、Firefox、Safari、Edge）

### ✅ 完整功能
- 🏫 教室布局自訂（2-10行列）
- 👥 CSV學生資料匯入
- 🎲 一鍵隨機分配座位
- 🔄 座位手動調整（點擊交換）
- 📊 即時統計資訊
- 📄 座位表匯出功能

### ✅ 美觀介面
- 現代化響應式設計
- 直觀的操作介面
- 即時視覺回饋
- 手機平板友善

## 🚀 快速開始

### 方法一：直接開啟
1. 雙擊 `index.html` 檔案
2. 瀏覽器會自動開啟程式

### 方法二：本地伺服器（推薦）
如果遇到檔案讀取問題，可以使用本地伺服器：

**使用Python（如果已安裝）：**
```bash
# Python 3
python -m http.server 8000

# Python 2
python -m SimpleHTTPServer 8000
```

**使用Node.js（如果已安裝）：**
```bash
npx http-server
```

然後在瀏覽器開啟 `http://localhost:8000`

## 📋 使用步驟

### 第一步：設定教室
1. 在左側「教室設定」區域
2. 輸入班級名稱（例：三年一班）
3. 設定教室行數（2-10）
4. 設定教室列數（2-10）
5. 點擊「建立教室」按鈕

### 第二步：匯入學生資料

#### 選項A：使用範例資料
- 點擊「建立範例資料」按鈕
- 系統會自動建立15位學生的範例資料

#### 選項B：匯入CSV檔案
1. 點擊「下載範例CSV」獲得格式範本
2. 準備您的CSV檔案，格式如下：
   ```
   學號,姓名,職務
   S001,王小明,班長
   S002,李小華,副班長
   ```
3. 點擊「點擊選擇CSV檔案」匯入

### 第三步：分配座位

#### 自動分配
- 點擊「🎲 隨機分配」按鈕
- 系統會自動隨機分配所有學生座位

#### 手動調整
1. 點擊任一座位（會變成粉紅色選中狀態）
2. 再點擊另一座位
3. 兩個座位的學生會自動交換

### 第四步：查看結果
- 右側顯示完整座位表
- 左側學生清單顯示分配狀態
- 底部統計卡片顯示分配進度

### 第五步：匯出座位表
- 點擊「📄 匯出座位表」按鈕
- 系統會下載CSV格式的座位表檔案

## 🎯 功能詳解

### 教室顯示
- **黑板**：顯示在座位表最上方
- **座位格**：顯示學生姓名和職務
- **空位**：顯示座位座標
- **選中座位**：粉紅色高亮顯示

### 學生清單
- **綠色邊框**：已分配座位的學生
- **灰色背景**：未分配座位的學生
- **座位資訊**：顯示在右側（行,列）

### 統計資訊
- **總學生數**：匯入的學生總數
- **已分配**：已分配座位的學生數
- **分配率**：分配完成的百分比

### 操作按鈕
- **🎲 隨機分配**：自動隨機分配所有座位
- **🔄 重置座位**：清空所有座位分配
- **📄 匯出座位表**：下載CSV格式座位表

## 📱 響應式設計

### 桌面版
- 左右分欄布局
- 完整功能顯示
- 最佳使用體驗

### 平板/手機版
- 上下堆疊布局
- 座位格自動縮小
- 觸控友善操作

## 🔧 技術特色

### 純前端實現
- 使用HTML5 + CSS3 + JavaScript
- 無需後端伺服器
- 資料處理完全在瀏覽器中進行

### 現代化設計
- CSS Grid 布局
- Flexbox 響應式設計
- 漸層色彩和動畫效果
- 卡片式介面設計

### 檔案處理
- FileReader API 讀取CSV
- Blob API 生成下載檔案
- 完整的錯誤處理和驗證

## ❓ 常見問題

### Q: 為什麼無法匯入CSV檔案？
A: 
1. 確保CSV檔案使用UTF-8編碼
2. 檢查檔案格式是否正確（學號,姓名,職務）
3. 確保第一行是標題行

### Q: 座位表顯示不正常？
A: 
1. 檢查瀏覽器是否支援現代CSS
2. 嘗試重新整理頁面
3. 確認教室行列數設定正確

### Q: 無法下載檔案？
A: 
1. 檢查瀏覽器下載設定
2. 確認瀏覽器允許檔案下載
3. 嘗試不同瀏覽器

### Q: 手機上操作困難？
A: 
1. 使用橫向模式獲得更好體驗
2. 放大頁面以便點擊座位
3. 建議使用平板或電腦操作

## 🎉 使用技巧

### 快速操作
1. 使用「建立範例資料」快速測試功能
2. 先設定好教室大小再匯入學生
3. 隨機分配後再手動微調

### 最佳實踐
1. 學生人數不要超過座位數
2. CSV檔案保持簡潔格式
3. 定期匯出座位表備份

### 效率提升
1. 準備標準格式的CSV範本
2. 使用職務欄位標記重要學生
3. 善用統計資訊監控進度

---

## 📞 技術支援

如果遇到問題：
1. 檢查瀏覽器控制台錯誤訊息
2. 確認檔案格式和編碼
3. 嘗試重新整理頁面
4. 使用不同瀏覽器測試

**版本**: v1.0.0 網頁版  
**更新日期**: 2024年12月  
**相容性**: Chrome 60+, Firefox 55+, Safari 12+, Edge 79+ 